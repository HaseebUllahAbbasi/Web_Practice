/*

REPL :: Read, Evaulate, Print, Loop;
you may open cmd and wirte node then write as you are writing in browser;

FS : used to work with files 
Read files
Create files
Update files
Delete files
Rename files

fs.appendFile()
:: appends specified content to a file. If the file does not exist, the file will be created
fs.open()
:: takes a "flag" as the second argument, if the flag is "w" for "writing"
If the file does not exist, an empty file is created:

fs.writeFile()
::  replaces the specified file and content if it exists. If the file does not exist, a new file, containing the specified content, will be created.

fs.readFile() method is used to read files on your computer.

fs.unlink()::
method deletes the specified file:

fs.rename() 

for async:
the callback function is created in order to handle the data and the error 
or perform more operation :

for Sync: 
where as no need to add callback function then simple write Sync in the end of the async method and data can be returned like in read ..
but the wait is done in the sync;




*/

